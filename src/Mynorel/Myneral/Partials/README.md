# Myneral Partials & Macros

Register and render template partials/macros for reusable UI components.

- Use `PartialManager::register($name, $content)`
- Use `PartialManager::render($name, $context)`
